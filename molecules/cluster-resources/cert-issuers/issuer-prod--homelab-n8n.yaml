apiVersion: cert-manager.io/v1
kind: Issuer
metadata:
  name: letsencrypt-prod--homelab-n8n
spec:
  acme:
    email: mrsauravsahu@outlook.com
    server: https://acme-v02.api.letsencrypt.org/directory
    privateKeySecretRef:
      name: le-issuer-account-key--homelab-n8n
    solvers:
    - dns01:
        cloudflare:
          email: mrsauravsahu@outlook.com
          apiTokenSecretRef:
            name: cloudflare-apikey-secret
            key: apikey
      selector:
        dnsNames:
        - "n8n.mrsauravsahu.in"
