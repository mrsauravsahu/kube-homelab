flags:
  rewrite: true

cert:
  issuer: letsencrypt-prod
  secretName: le-issuer-payobills-cert

service:
  name: bills
  port:
    number: 80

ingress:
  name: bills-graphql
  subdomain: payobills
  routePath: /bills-graphql

hosts:
%{ for host in hosts ~}
- name: ${host}
%{ endfor ~}
