flags:
  rewrite: false

service:
  name: keycloak-http
  port:
    number: 80

annotations:
  hajimari.io/enable: "true"
  hajimari.io/appName: "keycloak"

ingress:
  subdomain: id
  routePath: /

hosts:
%{ for host in hosts ~}
- name: ${host}
%{ endfor ~}