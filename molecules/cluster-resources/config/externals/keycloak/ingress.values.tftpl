flags:
  rewrite: false

service:
  name: keycloak-http
  port:
    number: 80

annotations:
  hajimari.io/enable: "true"
  hajimari.io/appName: "idp"

ingress:
  subdomain: idp
  routePath: /

hosts:
%{ for host in hosts ~}
- name: ${host}
%{ endfor ~}