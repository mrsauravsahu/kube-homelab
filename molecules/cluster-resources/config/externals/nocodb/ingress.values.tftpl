flags:
  rewrite: false

service:
  name: nocodb
  port:
    number: 8080

annotations:
  hajimari.io/enable: "true"
  hajimari.io/appName: "nocodb"

ingress:
  subdomain: nocodb
  routePath: /

hosts:
%{ for host in hosts ~}
- name: ${host}
%{ endfor ~}