flags:
  rewrite: false

service:
  name: pihole-web
  port:
    number: 80

annotations:
  hajimari.io/enable: "true"
  hajimari.io/appName: "pihole"

ingress:
  subdomain: dns
  routePath: /

hosts:
%{ for host in hosts ~}
- name: ${host}
%{ endfor ~}