apiVersion: cert-manager.io/v1
kind: Issuer
metadata:
  name: letsencrypt-prod--homelab-jellyfin
spec:
  acme:
    email: mrsauravsahu@outlook.com
    server: https://acme-v02.api.letsencrypt.org/directory
    privateKeySecretRef:
      name: le-issuer-account-key--homelab-jellyfin
    solvers:
    - dns01:
        cloudflare:
          email: mrsauravsahu@outlook.com
          apiTokenSecretRef:
            name: cloudflare-apikey-secret
            key: apikey
      selector:
        dnsNames:
        - "media.mrsauravsahu.in"
